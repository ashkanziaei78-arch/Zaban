# شخصی‌سازی قبل از انتشار عمومی

هرچه اینجاست را در `site/index.html` عوض کنید، بعد `python3 build-dist.py` بزنید.

بندهای ۱ تا ۵ مربوط به سایت معرفی‌اند. بند ۶ مربوط به پنل است و **قبل از
اینکه کاربر واقعی وارد شود باید انجام شود.**

## ۱. شمارهٔ تماس  ← هنوز نیست

شمارهٔ ساختگی **حذف شده** تا کسی به شمارهٔ اشتباه زنگ نزند.
وقتی شماره‌تان آماده شد، در سه جا اضافه کنید:

| کجا | چه چیزی اضافه شود |
|---|---|
| فوتر، ستون «تماس» | `<li><a href="tel:+98XXXXXXXXXX" class="num" dir="ltr">۰۲۱-XXXXXXXX</a></li>` |
| پیام موفقیت فرم دمو | بعد از «یا همین حالا برایمان بنویسید» |
| دکمهٔ پایین صفحه | یک دکمهٔ «تماس بگیرید» کنار CTA نهایی |

## ۲. قیمت‌ها  ← عدد پیشنهادی من است، نه تصمیم شما

سه عدد در بخش تعرفه، هر کدام در دو جا (ماهانه و سالانه):

| پلن | ماهانه (`data-m`) | سالانه (`data-y`) |
|---|---|---|
| پایه | ۴۹۰٬۰۰۰ | ۳۹۲٬۰۰۰ |
| رشد | ۱٬۷۰۰٬۰۰۰ | ۱٬۳۶۰٬۰۰۰ |
| حرفه‌ای | ۴٬۴۰۰٬۰۰۰ | ۳٬۵۲۰٬۰۰۰ |

**دو جا باید عوض شود** وگرنه محاسبه‌گر با کارت‌ها نمی‌خواند:
1. در HTML: `data-m` و `data-y` روی هر `<b class="num">`
2. در جاوااسکریپت: آرایهٔ `PLANS` — فیلد `m` (به ریال کامل، بدون جداکننده)

منطق قیمت‌گذاری در `docs/K-monetization.md` است: پلن میانی حدود
۱ تا ۲ درصد درآمد ماهانهٔ یک آموزشگاه ۳۰۰ نفره.

## ۳. فرم درخواست دمو  ← به جایی وصل نیست

الان فقط اعتبارسنجی می‌کند و پیام موفقیت نشان می‌دهد. **هیچ‌جا ذخیره نمی‌شود.**
ساده‌ترین راه روی هاست پلسک: یک فایل `contact.php` بسازید که ایمیل بفرستد،
و در `site/index.html` تابع `submit` فرم را به آن `fetch` کنید.

## ۴. نماد اعتماد و ساماندهی

در فوتر دو جعبهٔ خط‌چین با نوشتهٔ «نماد اعتماد الکترونیکی» و «ساماندهی» هست.
وقتی گرفتید، کد لوگوی رسمی را جایگزین کنید. برای فعال‌سازی درگاه پرداخت
نماد اعتماد لازم است و گرفتنش چند هفته طول می‌کشد.

## ۵. آمار و نقل‌قول مشتری

عمداً هیچ آمار مشتری یا نقل‌قولی ننوشتم چون هنوز مشتری ندارید و ساختنشان
درست نیست. بعد از اولین آموزشگاه‌ها، بخش «داستان مشتری» را اضافه کنید.

---

## ۶. پنل — قبل از اولین کاربر واقعی

پنل روی `panel.talkora.ir` بالا می‌آید و بستهٔ جدایی دارد
(`talkora-panel.zip`). چک‌لیست کامل در `docs/U-checklist.md` است.

**دیگر هیچ فایلی را دستی ویرایش نمی‌کنید.** بعد از آپلود بسته، آدرس
`panel.talkora.ir/setup/` را باز کنید. نصب‌کننده جدول‌ها را می‌سازد،
`otp_pepper` را خودش تصادفی تولید می‌کند، حساب سوپرادمین اول را با
همان فرم می‌سازد، و فایل پیکربندی را — اگر پوشهٔ بیرون از webroot قابل
نوشتن باشد — آنجا می‌نویسد.

تنها چیزی که باید آماده داشته باشید، چهار مقدار اتصال دیتابیس است که
پلسک در Databases نشان می‌دهد:

| مورد | کجا از پلسک می‌آید |
|---|---|
| نام پایگاه داده | Databases — همان نامی که در فهرست می‌بینید، با پیشوند |
| نام کاربری | همان‌جا، آن هم معمولاً پیشوند دارد |
| رمز | همان رمزی که موقع ساخت کاربر گذاشتید |
| میزبان | تقریباً همیشه `localhost` |

کلید sms.ir و شناسهٔ قالب هم دیگر در فایل نیستند: از پنل سوپرادمین،
سربرگ «ورود و پیامک» وارد می‌شوند. تا وقتی قالب تأیید نشده، سامانه در
«حالت پل» کار می‌کند و کد ورود به‌جای پیامک در همان سربرگ دیده می‌شود.

### ۶ب. سوپرادمین — زیردامنهٔ سوم، بعد از پنل

مدیریت کل پلتفرم (آموزشگاه‌ها، نقش کاربرها، ورود به‌جای کاربر برای
پشتیبانی) دیگر داخل پنل نیست — یک زیردامنهٔ جداست: `admin.talkora.ir`،
بستهٔ جداگانهٔ خودش (`talkora-admin.zip`) و راهنمای خودش
(`راهنمای-سوپرادمین.txt` داخل همان بسته).

فقط بعد از نصب کامل پنل بالا بیاورید — جدول‌ها آنجا ساخته می‌شوند،
اینجا فقط از همان دیتابیس با نشستی جدا وارد می‌شود. همان حساب مدیری که
موقع نصب پنل ساختید، همینجا هم کار می‌کند.

### چیزهایی که نباید انجام دهید

- **بعد از نصب، پوشهٔ `setup` را روی هاست نگه ندارید.** نصب‌کننده از
  لحظه‌ای که فایل پیکربندی ساخته شد خودش را قفل می‌کند، ولی پاک‌کردنش
  یک لایهٔ دفاعی دیگر است.
- **فایل پیکربندی را در گیت نگذارید.** در `.gitignore` هست؛ همان‌جا بماند.
  رمز دیتابیس و کلید امضای کدهای ورود داخلش است.
- **پوشهٔ `_نصب-آپلود-نکنید` را روی هاست نبرید.** فقط نسخهٔ پشتیبانِ
  دستیِ ساخت جدول‌هاست، برای وقتی که نصب‌کننده کار نکند.
- **قبل از فعال‌شدن SSL کاربر واقعی نفرستید.** کوکی نشست پرچم `Secure`
  دارد و روی HTTP ست نمی‌شود، پس ورود بلافاصله باطل می‌شود.

### چه بخش‌هایی از پنل واقعی است و چه بخش‌هایی نیست

**واقعی:** ورود با کد یک‌بارمصرف، پیامک sms.ir، نشست، نقش کاربر،
محدودیت نرخ، ثبت رویداد.

**هنوز دادهٔ نمونه:** بقیهٔ صفحه‌ها — کلاس‌ها، حضور و غیاب، مالی، تکالیف،
نمره‌دهی. اسکیمای کاملشان در `server/db/001_init.sql` نوشته و روی
پستگرس آزمایش شده، ولی برای اجرا سرور مجازی می‌خواهد (بخش T.2).

یعنی می‌توانید همین حالا کاربر واقعی ثبت‌نام کنید و وارد شود — فقط به
او بگویید داده‌های داخل پنل نمونه‌اند.
