<?php
// اگر مستقیم از مرورگر باز شود، چیزی برنگرداند
if (realpath(__FILE__) === realpath((string)($_SERVER['SCRIPT_FILENAME'] ?? ''))) { http_response_code(404); exit; }
/**
 * پیکربندی پنل سوپرادمین تاکورا.
 *
 * این فایل را به config.php تغییر نام دهید و مقادیر را پر کنید.
 * اگر پلسک اجازه می‌دهد، بهتر است در پوشهٔ private/ بالاتر از httpdocs
 * زیردامنهٔ admin.talkora.ir بگذاریدش با نام talkora-config.php — آن‌وقت
 * اصلاً از وب قابل خواندن نیست.
 *
 * ⚠ config.php را هرگز در گیت نگذارید.
 *
 * ⚠⚠ دو مقدار زیر باید عیناً — حرف به حرف — همان چیزی باشد که در
 * config.php پنل آموزشگاه‌ها (panel.talkora.ir) گذاشته‌اید:
 *   - db_host / db_name / db_user / db_pass   (باید همان یک پایگاه داده
 *     باشد، وگرنه این پنل هیچ آموزشگاهی نمی‌بیند)
 *   - otp_pepper   (وگرنه کدهایی که از بخش «ورود و پیامک» این پنل
 *     می‌سازید، هیچ‌وقت در پنل آموزشگاه تأیید نمی‌شوند)
 */

return [
    // ── پایگاه داده — همان مقدارهای config.php پنل آموزشگاه‌ها ─────
    'db_host' => 'localhost',
    'db_name' => 'talkora',
    'db_user' => 'talkora',
    'db_pass' => '',

    // ── کلید HMAC کد یک‌بارمصرف — دقیقاً همان مقدار پنل آموزشگاه‌ها ──
    'otp_pepper' => 'CHANGE-ME-همان-مقدار-panel/api/config.php',

    // ── کلید راه‌اندازی اولین حساب سوپرادمین ────────────────────────
    // یک رشتهٔ تصادفی بلند، فقط برای همین پنل. با این دستور بسازید:
    //   php -r "echo bin2hex(random_bytes(24));"
    // بعد از ساخت اولین حساب، این کلید دیگر لازم نیست ولی پاک‌کردنش
    // ضروری نیست — bootstrap فقط وقتی هیچ سوپرادمینی نباشد کار می‌کند.
    'admin_setup_key' => 'CHANGE-ME-یک-رشتهٔ-تصادفی-۴۸-کاراکتری',

    // ── sms.ir — فقط جایگزین پیش‌فرض؛ منبع اصلی جدول site_setting است ──
    'smsir_api_key' => '',
    'smsir_template_id' => 0,
    'smsir_param_name' => 'CODE',

    // true یعنی پیامک واقعی ارسال نشود و کد در لاگ خطای PHP بیفتد.
    'sms_dry_run' => false,
];
